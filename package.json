{
  "name": "readflow-backend",
  "version": "1.0.0",
  "description": "ReadFlow 阅读器后端服务 - AI API代理与RAG支持",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node -c server.js && node -c app.js && node -c ai-service.js && node -c memory-service.js && node -c routing-service.js",
    "smoke": "node -e \"fetch('http://localhost:3000/api/health').then(r=>r.json()).then(j=>{if(j.status!=='ok')process.exit(1);console.log('health ok')})\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "sqlite3": "^5.1.6",
    "uuid": "^9.0.0",
    "wink-bm25-text-search": "^3.1.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "epub",
    "reader",
    "ai",
    "rag",
    "reading"
  ]
}
